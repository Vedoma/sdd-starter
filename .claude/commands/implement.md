# /implement - Phase 6: Guided Implementation

Load and obey [`constitution.md`](../../constitution.md) first (binding principles),
then follow the canonical prompt at [`prompts/implement.md`](../../prompts/implement.md). The prompt
file is the source of truth for this phase - do not duplicate its content here.

Usage: `/implement TASK-XXX`. Implements ONLY that task. Restate its acceptance criteria and self-check each before finishing. Spec overreach is a defect.

Present the result as a proposal and stop for human review before saving or committing.
